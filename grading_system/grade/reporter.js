// Print the report of a student with name, score, and grade

function printReport(name, score, grade){
    console.log(`Student report for: ${name}`);
    console.log(`Score: ${score}`);
    console.log(`Grade: ${grade}`);
}

module.exports = {printReport};
